const ADD = "add"
const DELETE = "delete"
const UPDATE = "update"

const ADDED_SUFFIX = "__added"
const DELETED_SUFFIX = "__deleted"

module.exports = {
  ADD,
  DELETE,
  UPDATE,
  ADDED_SUFFIX,
  DELETED_SUFFIX,
}
